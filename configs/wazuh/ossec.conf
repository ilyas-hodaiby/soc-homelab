<!-- Wazuh Manager Configuration -->
<!-- Location: /var/ossec/etc/ossec.conf -->

<ossec_config>

  <global>
    <email_notification>no</email_notification>
    <logall>yes</logall>
    <logall_json>yes</logall_json>
  </global>

  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/auth.log</location>
  </localfile>

  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/syslog</location>
  </localfile>

  <alerts>
    <log_alert_level>3</log_alert_level>
    <email_alert_level>10</email_alert_level>
  </alerts>

</ossec_config>
```

**Sur GitHub :**
- `soc-homelab` → Add file → Create new file
- Nom : `configs/wazuh/ossec.conf`
- Colle → Commit : `Add Wazuh manager base configuration`

---

## Fichier 3 — `configs/ssh-hardening.txt` (Enterprise Network)

**Copie tout ça :**
```
! ===== SSH Hardening Configuration =====
! Applied on all network devices
! Telnet disabled — SSH v2 only

! Generate RSA key
crypto key generate rsa modulus 2048

! Enable SSH v2
ip ssh version 2
ip ssh time-out 60
ip ssh authentication-retries 3

! Disable Telnet
line vty 0 4
 transport input ssh
 login local
 exec-timeout 10 0

! Restrict SSH to Management VLAN only
access-list 99 permit 192.168.40.0 0.0.0.31
access-list 99 deny any log
line vty 0 4
 access-class 99 in

! Local user for SSH
username admin privilege 15 secret STRONG_PASSWORD
